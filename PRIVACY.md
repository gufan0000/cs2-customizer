# 隐私说明

**一句话版本：装上就用，程序默认不向任何服务器发送任何数据。**
本文档描述的每一条对外通信，要么**默认关闭需要你自己去打开**，要么**每次都要你点一下才发生**。

本文所有内容都可以在源码里对着看——文中标注了对应文件，欢迎核对。
说得和代码不一致，那是缺陷，请开 issue。

---

## 一、你的数据存在哪儿

全部在你自己的电脑上，明文，随时可以看、可以删：

| 内容 | 位置 |
| --- | --- |
| 全部设置（含准心、音效、快捷键、目录等） | `%LOCALAPPDATA%\FanTool\config.json` |
| 运行日志 | `%LOCALAPPDATA%\FanTool\logs\` |
| 页面打开次数（给侧栏「常用」排序用） | `%LOCALAPPDATA%\FanTool\usage\page_usage.json` |
| 道具瞄点使用频次 | `%LOCALAPPDATA%\FanTool\` 下的数据文件 |
| 你导入的素材 | 你选择的目录 / 程序数据目录 |
| 写给游戏的配置 | 你的 CS2 安装目录下的 `cfg` 文件夹 |

想彻底清除：退出程序后删除 `%LOCALAPPDATA%\FanTool` 整个目录即可
（这会同时清掉设置，卸载不会自动删它，是为了重装后配置还在）。

**没有账号、没有服务器上的用户档案，也就没有「注销账号」这回事。**

---

## 二、匿名使用统计（默认**关闭**，opt-in）

代码：`core/usage_reporter.py`　入口：设置 →「高级设置」→「匿名使用统计」

### 默认状态

`usage_report_enabled` 默认为 `False`。**你不去勾那个复选框，这个模块连一个后台线程都不会起**
（`schedule_startup_report()` 第一件事就是检查开关，没开直接返回）。

### 开启后到底发什么

只有下面这四个字段，**白名单之外什么都不会发**：

| 字段 | 内容 | 说明 |
| --- | --- | --- |
| `install_id` | 一个 `uuid4` 随机十六进制串 | 首次开启时生成并存在本地配置里。它与账号、硬件、Steam ID、机器名**都无关**，随机数一个；删掉配置文件就换一个新的 |
| `version` | 软件版本号，如 `2.2.2` | 用来知道某个问题集中在哪个版本 |
| `page_opens` | 各页面被打开的**次数** | 例如 `{"crosshair": 42, "music": 3}`，用来判断哪些功能没人用 |
| `switches` | 7 个功能开关的开/关状态 | `crosshair` / `magnifier` / `flash` / `osd` / `map_preset` / `nav_frequent` / `expert_mode` |

**不包含**：账号、昵称、邮箱、Steam ID、机器名、IP（服务端能看到来源 IP，这是 HTTP 的物理事实，
但程序不会主动上报任何标识）、系统信息、硬件信息、**任何文件路径**、你的具体设置数值、
游戏对局数据、素材内容或文件名。

> 补充一个诚实的细节：本地的 `page_usage.json` 里除了次数还存了「最近使用时间」（给侧栏排序做时间衰减用），
> **上报时只取次数那一项**，时间戳不出本机。

### 频率与失败处理

- 启动 60 秒后在后台线程发一次，**24 小时内最多一条**（`MIN_INTERVAL_SECONDS = 24*3600`）；
- 服务端没部署这个接口（404）或网络失败：**静默放弃**，不重试、不弹窗、不打扰你；
- 整个过程在后台线程里，不阻塞界面。

### 你可以先看再决定

「高级设置 → 匿名使用统计 → **查看将上报的内容**」按钮展示的，就是真正会被发出去的那份 JSON
（同一个 `build_payload()` 函数，不是另写一份给你看的样品）。看完不满意，关掉就是了。

### fork 需要注意

上报地址来自 `service_urls.OFFICIAL_WEBSITE_URL`，即原作者的服务器。
如果你 fork 了本项目并分发给别人，请把它改成你自己的端点或直接移除该模块——
**不要让你的用户在不知情的情况下把数据发给第三方。**

---

## 三、崩溃报告（**绝不自动上传**）

代码：`core/crash_reporter.py`，询问逻辑在 `gui_widget.py`

这是本项目最不想含糊的一条：**崩溃日志永远不会被自动上传，一次都不会。**

流程是这样的：

1. 程序启动后发现有**新的**崩溃日志（`%LOCALAPPDATA%\FanTool\logs\bootstrap_crash_*.log`，
   且 mtime 晚于上次处理过的时间）；
2. 弹一个**非模态**对话框（不会打断你正在做的事），并把**将要发送的内容前 20 行直接摊给你看**；
3. 三个按钮：**发送并帮助改进** / **本次不发** / **不再询问**；
4. 无论你选哪个，这批日志都被标记为已处理，不会反复骚扰你；选「不再询问」则永久关闭
   （`crash_report_prompt = "never"`，想开回来就改配置文件里这一项）。

选择发送时，发出去的只有：

- 崩溃堆栈**纯文本**（最多最近 3 份日志合并，整体截断到 64KB）；
- 版本号与客户端时间戳。

没有配置文件、没有素材、没有截图、没有标识符。发送失败或接口未部署时静默放弃，
**日志始终保留在你本地**——你随时可以自己打开看看要发的是什么。

---

## 四、日志与脱敏

代码：`core/utils/logger.py`、`core/utils/log_filter.py`

- 文件日志默认级别 **INFO**（不是 DEBUG）；需要排查问题时可临时开到 DEBUG：
  配置项 `debug_file_log = true`，或设环境变量 `FANPAI_DEBUG_LOG=1`；
- 日志按大小轮转，并**保留 14 天**自动清理，不会无限增长；
- 写入前会过一道脱敏过滤器 `SensitiveFilter`，覆盖：
  `token` / `access_token` / `refresh_token` / `api_key` / `password` / `secret` 等键值对、
  `Authorization: Bearer xxx`、Steam ID（只保留末 4 位）、
  Windows 用户目录 `C:\Users\<用户名>\` → `C:\Users\***\`、邮箱地址（保留域名）。

⚠️ 这个过滤器是**保守**设计：只处理明显的键值对形式，不会去猜纯数字或 base64（避免把时间戳、
文件大小误伤掉）。所以它**不保证覆盖一切**——把日志贴到公开 issue 之前，请自己再扫一眼。

日志只写在本地。除了上一节你亲手点「发送」的那种情况，没有任何代码路径会把它传出去。

---

## 五、本机网络行为：GSI 服务端

代码：`gsi_server.py`

CS2 的 Game State Integration 是 Valve 官方接口：游戏把「你现在是不是活着 / 手里拿的什么枪 /
刚才有没有击杀」这类状态 **POST 到本机的一个 HTTP 端口**，程序据此触发音效、动画等。

- 服务只监听 **`127.0.0.1`（本机回环）**，默认端口 3000，被占用时在 3001–3010 之间顺延；
  局域网内其他机器**连不上**它；
- 数据来自你自己的游戏客户端，在内存里消费掉，不落盘（DEBUG 日志会记录部分状态，见上一节）；
- 程序**只读**这个官方接口，**不读写游戏内存、不注入进程、不修改游戏文件**（除了你授权的 cfg 文件）。

同一台机器上的其他程序理论上可以向这个端口发伪造的游戏状态（这是本地回环服务的固有性质）。
最坏结果是让本程序播个音效或亮个准心动画——它不会把任何东西写回游戏、也不会向外发送。
如果你发现能做到更多，那是安全漏洞，请按 [SECURITY.md](SECURITY.md) 私下上报。

---

## 六、其余会联网的地方（全部由你主动触发）

| 行为 | 何时发生 | 目标 |
| --- | --- | --- |
| 下载 VB-Cable 虚拟声卡驱动包 | 你在「语音输出」页点击安装 VB-Cable 时 | `download.vb-audio.com`（VB-Audio 官方） |
| 打开外部链接 | 你点击关于页/帮助里的链接时 | 交给系统默认浏览器打开，程序本身不请求 |

除此之外，**没有**其他自动的对外请求。

---

## 七、开源版**没有**的东西

以下能力在开源版中不存在，相关代码已从仓库物理删除，不是「关掉了」而是「没有」：

- ❌ **账号 / 登录 / 鉴权**——没有账号体系，不收集邮箱、手机号、密码；
- ❌ **云端配置同步**——你的配置不会被上传到任何地方；
- ❌ **在线更新检查**——程序不会在启动时联网询问版本，也不会被远端「封杀」；
  升级请自行到发布页下载；
- ❌ **音乐平台在线解析 / 抓流下载**——音乐播放器**只播放你本地的音频文件**，不请求任何音乐平台；
- ❌ 广告、第三方统计 SDK、第三方分析组件——一个都没有，依赖清单可查
  （`requirements_qt.txt` / `requirements-ci.txt`）。

---

## 八、本机权限说明

| 权限 | 什么时候用 | 说明 |
| --- | --- | --- |
| 管理员权限 | 使用「开镜放大」时 | Windows Magnification API 的要求；不用这个功能就不需要提权 |
| 全局热键 | 你设置了快捷键时 | 监听按键组合以触发音板/预设切换等；只判断你配置的组合，不记录击键内容 |
| 开机自启 | 你在设置里开启时 | 写入 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`，关掉即删除 |
| 读写 CS2 目录 | 应用准心/视角等需要写 cfg 的设置时 | 只写本程序自己生成的 cfg 文件 |
| 麦克风 / 虚拟声卡 | 使用「语音输出」功能时 | 音频在本机内转发，不录制、不上传 |

---

## 九、儿童与第三方

本项目不面向 13 岁以下用户定向提供服务，也不主动收集任何年龄信息。
本项目不含任何第三方 SDK，不与广告商、数据经纪商共享数据——因为它压根没有数据可共享。

---

## 十、变更与联系

本说明如有变更，会随版本更新在仓库中留下 git 历史，可直接比对。
对本说明有疑问、或发现程序行为与本文不符：

- 一般问题：开 issue；
- 涉及安全/隐私漏洞：请走 [SECURITY.md](SECURITY.md) 的私密上报通道；
- 邮件：`<维护者邮箱>`（**维护者注意：发布前替换成真实邮箱**）。
