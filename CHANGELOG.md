# 更新日志

本文件记录本项目所有值得注意的改动。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]

暂无。

---

## [2.2.2] - 2026-08-12

**开源版的第一个版本。**

在此之前，本软件以「帆派助手 / FanTool」的名义闭源发布。本版本把它的完整源码以
GPL-3.0-or-later 开放出来，同时去掉了几块不适合开源分发的功能。
开源版是原版的**功能子集**——去掉的部分不会再回来。

本版同时包含 2.2.1 之后积累的功能改进，所以下面既有"开源化带来的变化"，
也有"相对 2.2.1 的常规改动"。

### 新增

- **完整源码公开**，任何人都可以查看、修改、再发布（遵守 GPL-3.0-or-later）。
  补齐了许可证全文、版权与商标声明、第三方依赖清单。
- **顶栏搜索（Ctrl+F）大改。** 以前下拉框和回车走的是两套不同的匹配：
  拼音（`zx` / `zhunxin`）、打错字（`dukcing`）、口语（`声音太大`）
  只在回车时有效、下拉里一条都不显示，看起来就像搜不到。现在两边是同一个引擎，
  结果按相关度排序。同时支持直接定位并高亮到具体某一行开关，而不只是跳到某一页；
  搜不到时会给出最近搜索与常去页面，不再只弹一句"没有匹配"。
- **搜索容错增强。** 换个说法也能搜到（"准星"能找到界面上写的"准心"、
  "鼠标速度"→ 灵敏度、"话筒"→ 麦克风、"生命值"→ 血量、"键位"→ 快捷键）；
  中文长词会自动拆开匹配，不用手动打空格；整块设置的卡片标题也能搜到；
  下拉关掉后点回搜索框或按 ↓ 就能重新展开，不用把字删光重打。

### 变更

- **音乐播放器只播放本地文件。** 你可以把自己的音乐文件加进播放列表照常使用，
  播放、切歌、音量、以及"活着时压低音量、死了恢复"这套联动都不受影响。
- **没有任何内置素材也能正常用。** 程序不再随包附带音效、语音、击杀图标等素材文件；
  用软件内的「资源导入向导」把你自己的素材导进来即可。没导入时程序照常启动，
  只是相关功能没有可选内容。
- **准心改用 Qt 渲染。** 开关跟手（从几百毫秒降到毫秒级），并新增 T 形准心样式。
- **启动更快。** 显示窗口前的关键路径从约 1.45 秒降到约 0.83 秒。
- **退出不再卡住。** 闪光子进程停止从约 2 秒降到 0.08 秒；强制结束主进程后，
  子进程不再变成孤儿留在后台。
- **排版与可读性大修。** 27 个页面在完整/紧凑两种窗口形态下都不再溢出或截断；
  修复 9 套主题下的文字对比度不足；页头统一；键盘 Tab 顺序按阅读顺序走。

### 移除

- **账号登录**。开源版不需要登录，打开就能用，所有功能不再有账号门槛。
- **云端配置同步**。配置只保存在你自己的电脑上。跨设备搬配置请用
  「配置快照」导出，或直接复制配置文件。
- **在线更新检查**。软件不会再联网查版本，也不会因为版本旧而被停用。
  想升级请自行到项目页面获取新版本。
- **在线音乐下载**。原先可以粘贴音乐平台或视频网站的链接下载歌曲，
  这部分能力已整体去掉，原因见上面「音乐播放器」一条。
- **随包分发的第三方素材**。音效、语音包、击杀图标、字体等涉及他人版权的文件
  不再包含在项目里。

### 修复

- **安装后什么都点得动，但什么都不生效。** 安装包里误带了打包用机器的配置文件，
  新电脑第一次启动会把它当成你的配置，游戏目录被写成别人机器上的路径，
  于是所有需要写配置文件的功能（音效、HUD、GSI、准心同步）全部静默失效；
  更糟的是这份配置恰好缺一个字段，导致首次使用引导也被跳过，
  你连"没设置游戏目录"都看不出来。本版启动时会自动识别这种被写脏的配置，
  清掉无效的游戏目录并重新引导你完成设置（只在确认是脏配置时才动，正常配置不受影响）。
- **一个坏掉的配置值会让软件彻底打不开**——没有窗口、没有报错、没法自救。
  现在坏值会被安全忽略并回落到默认值。
- **开了「按地图自动切预设」会把你的快照吃光。** 每换一张图就自动存一份快照，
  存满 20 份就按时间删旧的，结果你手动存的快照、以及"恢复配置前自动帮你留的后悔药"
  会被一起删掉。实测三张图的规则、一晚四局，第五个晚上就全没了。
  现在优先淘汰自动快照，你自己存的和恢复用的后悔药会被保住。
- **回合音效会被上一段的淡出掐掉。** 胜利/失败音效如果比较长，
  它的淡出计时会在下一回合的开始音效播到一半时把声音切断。现在淡出只管自己那一段。
- **新音效第一次触发时队友听不到**（开了音效转发的情况）。没被预加载到的音效，
  第一次播放只有自己听得见，第二次才正常。
- **换耳机后音板自己听不到声。** 本地试听写死在开软件那一刻的设备上，
  中途拔耳机或插 USB 耳机后声音还从旧设备出。现在跟随系统当前的默认输出设备。
- **语音输出用的虚拟声卡驱动下载会卡死。** 对方服务器断流时永久停在"正在下载"，
  既不失败也没法重试，只能重启软件。现在会超时并明确报错。
- **预设应用前的快照没建成，界面还说"可在配置快照页回滚"。**
  现在建不成会如实告诉你，不再骗你有还原点。
- **「关于」页复制出来的诊断信息里，GSI 状态永远显示"未知"**——
  而这恰恰是排障最该看的一行。现在会如实显示运行状态与端口。
- **三处"谎报成功"**：开镜灵敏度写入失败仍提示"已同步"、GSI 端口自适应后配置重写
  实际 100% 失败却记成功、资源迁移失败被静默吞掉却写"已完成"标记
  （导致同版本内永不重试）。
- **退出游戏后 HUD 效果线程不再无限重写配置文件**（原来的退出判断永远不成立）。
- **崩溃日志变得可用**：日志会自压缩，无害的首次异常不再淹掉真正的崩溃现场。

---

## 开源之前的版本

以下条目提炼自闭源时期「帆派助手」的更新说明，保留在这里是为了让你知道
当前这份代码是怎么长成现在这样的。已经去掉了开源版不再具备的功能相关条目。

> 说明：这些版本从未以本项目的名义发布过。日期取自原仓库的版本标记；
> 2.1.2 / 2.1.3 / 2.1.4 的标记是事后一次性补打的，日期不代表各自的实际发布日；
> 2.1.1 及更早没有留下准确日期，为估算值。

---

## [2.2.1] - 2026-07-13

### 修复

- **根治击杀音效偶发丢失**，三条会确定性丢音的路径全部修好：
  爆头声没播完时补枪（第二杀被播放策略丢弃）、中途加入或断线重连导致回合判定卡死
  整个回合、以及游戏配置缺少某个数据组件时回合结束补枪必丢。
- **系统性排查同类"事件丢失"问题**：回合胜利/失败/MVP 音效整块丢失、
  同队连胜时残留状态干扰、互杀时被击杀音效被压制吞掉、连投手雷第二声被吞、
  被闪瞬间游戏崩溃导致自定闪光永久留在屏幕上、道具瞄点改键后残留幽灵热键。
- **观战数据污染**（一类根因）：死亡后观战队友时，会把队友的击杀、死亡、血量、MVP
  当成你自己的，导致会话统计、HUD 配色、特殊音效、死亡音效、音乐联动全都乱触发。
  现已统一按本人身份过滤。
- **音乐播放器**：拖动进度后暂停再恢复会倒退、删歌后索引错位、切列表取错曲子、
  播放回调跨线程刷界面。
- **闪光独立进程**：停止后再启动的进程停不掉、累计崩溃后功能永久失效、
  父进程崩溃后闪光全屏留屏。
- **语音按键说话**：加载失败时误放开其他功能的开麦键、兜底松键后带着静音的麦克风发声；
  麦克风静音改为引用计数，播放取消改独立令牌。
- **渲染层**：准心销毁后残留僵尸线程、改分辨率后击杀图标错位、多屏下道具窗口隐藏失败。
- **安全加固**：导入的配置分享文件按字段白名单过滤（防止被改写游戏目录）、
  准心预设数值注入钳制、调试密码改哈希存储、虚拟声卡驱动下载做校验、快照索引原子写入。
- **功能页**：放大镜加载覆盖会清空用户校准、语音稀疏槽位配置丢失、
  多个页面跨线程操作界面控件、测试按钮变黑。
- 次要修复：过渡防抖从 0.8 秒降到 0.3 秒且开火推断放行首杀；
  缓存淘汰不再误停共享别名正在播的声音。

### 新增

- **一步式"新建风格"向导**：输入风格名 → 拖入任意命名的音频 → 自动改名落盘 → 自动刷新，
  覆盖击杀音效/语音、切枪、换弹、被击杀五个页面。
- **导入向导容错**：未识别的文件可以手动归类为新风格；失败原因中文化并附标准目录示例。
- **音频体检补完整性检查**：直接告诉你某个风格缺哪几个连杀文件（回答"为什么不响"）。
- 试听支持选 1–5 连杀档位；风格支持重命名和安全删除（移入回收区、配置引用自动同步）；
  进入音效页时自动重扫风格列表。

### 移除

- 清理 3 个已经没人用的模块（旧道具菜单控制器、旧虚拟声卡安装器、重复的配置实现）。

---

## [2.2.0] - 2026-06-12

### 变更

- **启动提速约 3 倍。** 打包形态从单文件改为目录形态，冷启动从 5.9 秒降到约 1.9 秒，
  安装版实测约 1.45 秒。
- **点击更跟手。** 准心和开镜放大的开关改为复用窗口，开关延迟从几百毫秒降到毫秒级。
- **根治启动时"闪一下 / 冻一下"**：定位并消除了初始重绘与逐页构建造成的卡顿。

### 新增

- **现代化安装包**：中文安装向导，普通权限安装、升级时自动关闭旧版、卸载保留用户配置。
- **配置分享文件**（`.cs2customizer`）：可以把自己的配置发给别人，导入时有多项安全校验。
- **按地图自动切换预设**、**游戏内 OSD 提示**。
- 界面字号缩放（1.0 / 1.1 / 1.25）、设置项拼音搜索、应用内快捷键、
  侧栏「常用」分组、操作撤销。

---

## [2.1.4] - 2026-06-10

### 新增

- **全局热键注册中心**：开镜放大、语音音板、道具瞄点统一注册，撞键时给出冲突提示；
  「高级设置」新增热键总览。
- **首次安装三步快速上手引导。**

### 修复

- 改快捷键时可能误关其他功能全局热键的问题；补全放大镜与语音输出页的关闭清理。
- 退出流程改为步骤化并加耗时监控，避免个别清理卡住导致软件关不掉。
- GSI 端口被占用时自动改用 3001–3010。
- 首页等多处百分比显示更稳健。

### 变更

- 空状态页面补充了操作入口；界面上的状态标记更克制。

---

## [2.1.3] - 2026-06-10

### 变更

- **默认以普通权限运行。** 移除启动时的强制管理员提权，日常使用零 UAC 弹窗；
  开镜放大在普通权限下实测可用。
- 「高级设置 → 运行权限」新增当前权限显示和「以管理员身份重启」一键入口
  （只有少数环境需要）。
- 发布包默认关闭 UPX 压缩，显著降低杀毒软件与 SmartScreen 误报概率（体积略增）。

### 修复

- 放大镜初始化失败时支持重试，并主动引导提权重启，不再误导性地要求管理员。

---

## [2.1.2] - 2026-06-10

### 修复

- 首页音量在历史脏配置下显示「10000%」（加载时把音量钳制回 0–1）。
- 矮窗口下高级设置页、语音输出页内容压扁、控件被裁切（补外层滚动）。
- 任务栏图标归类不稳（改为与版本号无关的固定标识，跨版本保持一致）。

---

## [2.1.1] - 2026-05-08

### 变更

- **界面美学重做**：配色体系重构、引入矢量图标、提供 9 套主题。
- **架构统一**：18 个页面里各自复制的卡片实现收敛到统一组件，
  改一处配色能 100% 穿透到所有页面。
- 功能页面扩充到 27 个。
- 优化软件体积、窗口图标与界面细节，修复若干已知问题。

### 移除

- 清理死代码。

---

## [2.0.0] - 2026-04-19

### 变更

- **完全迁移到 PySide6**，界面整体现代化重做。
- 优化启动速度（快速启动 + 懒加载）、动态帧率优化。

### 新增

- 27 个功能页面。
- 全局音乐控制栏、智能按键说话会话复用、完整的日志系统。
