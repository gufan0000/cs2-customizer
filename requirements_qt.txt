# CS2 Customizer 2.0 - Qt版本依赖
# 现代化UI所需的额外依赖

# Qt框架
PySide6>=6.6.0

# v5 Phase 5: 矢量 icon 系统(Material Design Icons)
qtawesome>=1.4.0

# Windows 进程级音量 Ducking（枪声替换增强，可选）
pycaw; platform_system == "Windows"
comtypes; platform_system == "Windows"

# --- 2026-06-10 安全下限（pip-audit 体检后确定）---
# pillow 12.1.x 含 6 个图片解析 CVE（本软件处理用户导入图片，属真实攻击面）
pillow>=12.2.0
# requests/urllib3 升级修复已知 CVE（更新检测/账号登录链路使用）
requests>=2.34.2
urllib3>=2.7.0

# 注意：以下依赖已在 requirements.txt 中，这里仅列出Qt专用的
# - customtkinter (经典版本需要)
# - pygame (音频系统)
# - flask (GSI服务器)
# - 等等...

# --- 2026-08-14 死亡刷短视频 ---
# 通过浏览器调试端点注入 JS，把抖音从"精选网格页"切到"推荐视频流"。
# 抖音登录后会固定重定向到精选页，URL 参数覆盖不掉、合成鼠标消息也点不动，
# 这是唯一可行的方式。缺这个库只会退化成停在精选页，不影响其他功能。
websocket-client>=1.9.0
