# 安全策略 / Security Policy

## 请走私下通道上报，不要开公开 issue

发现安全问题请使用 GitHub 的 **Private vulnerability reporting**（私密漏洞上报）：

> 本仓库首页 → **Security** 标签页 → **Report a vulnerability**

如果该入口不可用，请发邮件到 `<安全联系邮箱>`（**维护者注意：发布前把这个占位符换成真实邮箱**），
标题带 `[SECURITY]`。

**为什么这条很重要**：本项目在用户机器上做的这几件事天然带攻击面——

- 起一个本地 HTTP 服务接收 CS2 的 GSI 回调（Flask，监听 `127.0.0.1`，端口 3000 起顺延）；
- 读写 CS2 目录下的 `.cfg` 文件，并在用户目录写配置与日志；
- 解析用户导入的第三方素材（图片 / 音频 / 压缩包）；
- 部分功能会请求管理员权限（开镜放大用的 Windows Magnification API）。

在公开 issue 里贴一份可复现的 PoC，等于在补丁发布之前把它推送给所有装了这个程序的人。
**先私下报，修好再公开**，这对用户和你都更好。

## 一份好报告包含什么

- 受影响的版本（帮助 → 关于 里能看到，或 `config.py` 的 `VERSION`）与操作系统版本；
- 攻击前提：需要本机权限吗？需要用户主动导入某个文件吗？需要局域网内其他机器吗？
- 复现步骤，最好能到「最小可复现」；
- 影响面：能读到什么、能写到什么、能不能拿到代码执行；
- 你认为的修复方向（可选，但很有帮助）。

⚠️ **报告里不要附带未脱敏的日志。** 程序日志已有脱敏过滤器（会遮蔽 token / api_key / password /
`Authorization: Bearer` / Steam ID 后 4 位以外的部分 / `C:\Users\<用户名>\` / 邮箱地址），
但它是**保守**策略，只处理明显的键值对形式，不保证覆盖一切。贴之前请自己再扫一眼。

## 响应时间承诺

这是一个由个人维护的开源项目，下面是能实际做到的承诺，不是漂亮话：

| 阶段 | 时限 |
| --- | --- |
| 确认收到报告 | **7 天内** |
| 给出初步定性（是否成立、严重级别、是否在范围内） | 14 天内 |
| 给出修复计划或明确说明不修及理由 | 30 天内 |
| 协调披露（修复发布后或与你商定的时间） | 默认 90 天上限 |

如果你在 14 天内没有收到任何回复，可以在 issue 区开一个**不含技术细节**的帖子催一下
（例如「我在 X 月 X 日通过私密通道提交了一份安全报告，请确认收到」）。

修复发布时会在更新日志中致谢报告者，你也可以要求匿名。本项目没有漏洞赏金。

## 支持的版本

| 版本 | 是否提供安全修复 |
| --- | --- |
| `master` 最新提交 | ✅ |
| 最新发布版（当前 2.2.x） | ✅ |
| 更早的发布版 | ❌ 请先升级到最新版复现 |

修复只会推进到最新版本，不做旧版本回迁。

## 在范围内（欢迎上报）

- **GSI 本地服务端**：未经校验的请求体导致崩溃/资源耗尽、监听地址意外扩大到 `0.0.0.0`、
  同机其他进程能否伪造/窃取游戏状态数据；
- **cfg 与文件写入路径**：路径穿越、能被诱导写到游戏目录以外的位置、写入内容可被外部注入；
- **素材导入**：恶意压缩包（zip slip / 解压炸弹）、恶意图片或音频文件导致的越界读写或任意文件落地；
- **提权路径**：请求管理员权限的流程能否被劫持（DLL 劫持、可写目录下的可执行文件、未校验的下载包）；
- **本地数据保护**：配置/日志文件权限过宽、脱敏过滤器可被绕过导致凭据落盘；
- **持久化项**：开机自启注册表项（`HKCU\...\Run`）可被指向其他程序；
- **网络行为**：出现任何**未经用户明示开启或主动触发**的对外请求（这在本项目里就算漏洞，
  详见 [PRIVACY.md](PRIVACY.md)）；
- **打包产物**：发布包被引入的供应链问题、依赖中确实能在本项目代码路径上触发的 CVE。

## 不在范围内（会直接关闭）

- **反作弊绕过、VAC 规避、任何形式的作弊功能**。本项目只使用 Valve 官方 GSI 接口 + 读写 cfg 文件，
  **不读写游戏内存、不注入进程、不提供竞技优势**。「如何让这个工具做到 X（X 是作弊）」不是安全报告，
  这类 issue 会被直接关闭。
- **CS2、Steam 或 Windows 自身的漏洞**。请报告给 Valve / Microsoft。
- **需要攻击者已经拥有本机管理员权限或物理访问才能利用的问题**。到那一步他能做的事远不止改这个程序的配置。
- **用户自己把配置改坏、把素材放错目录**导致的功能异常——这是 bug，走公开 issue 即可。
- **对任何线上服务的扫描或渗透测试**。本项目默认不连接任何服务器（见 `service_urls.py`）；
  上游闭源版所使用的线上服务不属于本仓库，也**未授权**任何形式的主动测试。
- **纯粹来自依赖清单的 CVE 报告**（例如「你依赖的某库有 CVE-XXXX」）而没有说明它在本项目的哪条代码路径上可触发。
  这类信息欢迎，但会按普通 issue 处理。
- 缺少某种加固措施但没有具体利用场景的「最佳实践」建议（可以开普通 issue 讨论）。

## 关于 fork

本项目以 GPL-3.0 发布，你 fork 之后就是你自己的软件了。如果你的 fork 分发给了别人，
请自行为你的用户提供安全上报通道，并把本文件中的联系方式换成你的——**不要把用户指向本仓库**。
